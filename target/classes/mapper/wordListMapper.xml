<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yasasitango.www.dao.WordListMapper">

	<select id="seeWordList" parameterType="hashmap" resultType="hashmap">
		   
		select 
			a.word_level
			,a.hiragana
			,a.kanji
			,a.korean
			,a.example
			,a.example_korean
			,to_char(b.searched_date, 'yyyy-mm-dd') as searched_date
		from  yasashi_tango a, yasashi_searched b
		where a.word_num = b.word_num and b.id=#{id}
		  
		<if test="searchType == 'date'">
	      and to_char(b.searched_date, 'yyyymmdd')= #{searchWord}
	       <!--나중에 date로 바꿔  -->
		</if>
		<if test="searchType == 'level'">
			and a.word_level = #{searchWord}
			<!--회원가입 기능 완성되면세션에서 모델에넣고 가져오세요  -->
	        
		</if>    
		order by searched_date desc
		
	</select>
</mapper>
