<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yasashitango.www.mapper.TangoMapper">

<select id="tangoResultKO" parameterType="String" resultType="com.yasasitango.www.vo.TangoVO">
    SELECT 
        word_level,
        hiragana,
        kanji,
        korean,
        example,
        example_korean
    FROM yasashi_tango
    WHERE korean LIKE '%'||#{korean}||'%' 
</select>

<select id="tangoResultJP" parameterType="String" resultType="com.yasasitango.www.vo.TangoVO">
    SELECT 
        word_level,
        hiragana,
        kanji,
        korean,
        example,
        example_korean
    FROM yasashi_tango
    WHERE kanji = #{kanji} OR hiragana = #{hiragana}
</select>

<insert id="insertSearched" parameterType="String">
    INSERT INTO yasashi_searched(
        searched_number,
        searched_word
    ) VALUES(
        searhced_num.nextval(),
        ${kangi}
    )
    
</insert>
</mapper>